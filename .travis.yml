language: node_js
node_js: "6"

# S: Build Lifecycle
install:
  - npm install


#before_script:
 # - npm install -g gulp

script:
  - hexo clean
  - hexo g
  - cp CNAME ./public/

after_script:
  - cd ./public
  - git init
  - git config user.name "xqx930807"
  - git config user.email "xqx930807@gmail.com"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master
# E: Build LifeCycle

branches:
  only:
    - source
env:
 global:
   - GH_REF: github.com/xqx930807/xqx930807.github.io.git
